# Browser calls go directly to api.propulence.com (CORS allowed in Django settings).
# NOTE: production.py's CORS_ALLOWED_ORIGINS does not yet include this app's deploy origin —
# a one-line backend config addition is needed before this actually works in production.
VITE_API_BASE_URL=https://api.propulence.com
VITE_APP_TITLE=Propulence Admin
